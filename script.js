const h=document.querySelector('.site-header'),m=document.querySelector('.menu-toggle'),n=document.querySelector('.nav-links');addEventListener('scroll',()=>h?.classList.toggle('scrolled',scrollY>25));m?.addEventListener('click',()=>n?.classList.toggle('open'));document.querySelectorAll('.faq-question').forEach(b=>b.addEventListener('click',()=>{const i=b.parentElement,a=i.querySelector('.faq-answer'),o=i.classList.contains('open');document.querySelectorAll('.faq-item.open').forEach(x=>{x.classList.remove('open');x.querySelector('.faq-answer').style.maxHeight=null});if(!o){i.classList.add('open');a.style.maxHeight=a.scrollHeight+'px'}}));const t=new Date('2027-07-18T16:00:00');function tick(){const d=Math.max(0,t-new Date()),v={days:Math.floor(d/86400000),hours:Math.floor(d/3600000)%24,minutes:Math.floor(d/60000)%60,seconds:Math.floor(d/1000)%60};Object.entries(v).forEach(([k,x])=>document.querySelectorAll(`[data-${k}]`).forEach(e=>e.textContent=String(x).padStart(2,'0')))}tick();setInterval(tick,1000);document.querySelector('#rsvp-form')?.addEventListener('submit',e=>{e.preventDefault();const s=document.querySelector('.form-status'),name=e.currentTarget.name.value.trim();s.textContent=name?`Thank you, ${name}. This preview form still needs to be connected to an RSVP service before launch.`:'Please enter your full name.'});
